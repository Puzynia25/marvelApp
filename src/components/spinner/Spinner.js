import spinner from '../../resources/img/spinner.gif';

const Spinner = () => {
    return (
        <img src={spinner} alt="spinner" style={{display: 'block', margin: '0 auto', width: '120px', height: '120px'}}/>
    )
}

export default Spinner;